// *** GENERATED BY PRISMIX :: DO NOT EDIT ***
datasource db {
    provider  = "postgresql"
    url       = env("DATABASE_URL")
    directUrl = env("DIRECT_URL")
}

generator client {
    provider        = "prisma-client-js"
    previewFeatures = ["fullTextIndex"]
}

model AdminAccount {
    adminAccountId Int           @id @default(autoincrement())
    name           String
    bankName       String
    balance        Float
    accNo          String        @unique
    isDelete       Boolean       @default(false)
    createdAt      DateTime      @default(now())
    updatedAt      DateTime      @updatedAt
    booking        Booking[]     @relation(name: "AdminAccountToBooking")
    installment    Installment[] @relation(name: "AdminAccountToInstallment")
}

model BankPayment {
    paymentId     String   @id @default(uuid())
    bookingId     String
    booking       Booking  @relation(name: "BankPaymentToBooking", fields: [bookingId], references: [bookingId])
    bankName      String
    accountNumber String
    amount        Float
    createdAt     DateTime @default(now())
    updatedAt     DateTime @updatedAt
}

model Booking {
    bookingId        String           @id @default(uuid())
    bankPayment      BankPayment[]    @relation(name: "BankPaymentToBooking")
    projectId        String
    project          Project          @relation(name: "BookingToProject", fields: [projectId], references: [projectId])
    plotNo           String
    area             Float
    totalAmt         Float
    paidAmt          Float
    remainAmt        Float
    installmentAmt   Float
    paymentType      PaymentType      @default(CASH)
    paymentStatus    PaymentStatus    @default(PARTIAL)
    adminAccountId   Int?
    adminAccount     AdminAccount?    @relation(name: "AdminAccountToBooking", fields: [adminAccountId], references: [adminAccountId])
    reminderDate     DateTime
    installmentDate  DateTime
    installmentCount Int
    dastavejAmt      Float
    referralId       String?
    referral         Referral?        @relation(name: "BookingToReferral", fields: [referralId], references: [referralId])
    isDelete         Boolean          @default(false)
    createdAt        DateTime         @default(now())
    updatedAt        DateTime         @updatedAt
    penalty          BookingPenalty[] @relation(name: "BookingToBookingPenalty")
    cashPayment      CashPayment[]    @relation(name: "BookingToCashPayment")
    chequePayment    ChequePayment[]  @relation(name: "BookingToChequePayment")
    customer         Customer[]       @relation(name: "BookingToCustomer")
    installment      Installment[]    @relation(name: "BookingToInstallment")
    upiPayment       UpiPayment[]     @relation(name: "BookingToUpiPayment")
}

model Project {
    projectId     String          @id @default(uuid())
    booking       Booking[]       @relation(name: "BookingToProject")
    expense       Expense?        @relation(name: "ExpenseToProject")
    miscExpense   MiscExpense[]   @relation(name: "MiscExpenseToProject")
    name          String
    description   String?
    ownerName     String
    area          Float
    unit          String
    status        ProjectStatus   @default(UPCOMING)
    address1      String
    address2      String?
    pincode       String
    logoUrl       String?
    location      String
    downPayment   Float?
    emiAmt        Float?
    totalAmt      Float?
    isDelete      Boolean         @default(false)
    createdAt     DateTime        @default(now())
    updatedAt     DateTime        @updatedAt
    projectImages ProjectImages[] @relation(name: "ProjectToProjectImages")
}

model Referral {
    referralId String    @id @default(uuid())
    booking    Booking[] @relation(name: "BookingToReferral")
    firstName  String
    lastName   String
    phone      String    @unique
    email      String?
    address    String?
    createdAt  DateTime  @default(now())
    updatedAt  DateTime  @updatedAt
}

model BookingPenalty {
    penaltyId   String   @id @default(uuid())
    amount      Float
    description String
    isComplete  Boolean  @default(false)
    bookingId   String
    booking     Booking  @relation(name: "BookingToBookingPenalty", fields: [bookingId], references: [bookingId])
    createdAt   DateTime @default(now())
    updatedAt   DateTime @updatedAt
}

model CashPayment {
    paymentId String   @id @default(uuid())
    bookingId String
    booking   Booking  @relation(name: "BookingToCashPayment", fields: [bookingId], references: [bookingId])
    amount    Float
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}

model ChequePayment {
    paymentId    String   @id @default(uuid())
    bookingId    String
    booking      Booking  @relation(name: "BookingToChequePayment", fields: [bookingId], references: [bookingId])
    chequeNumber String
    bankName     String
    amount       Float
    createdAt    DateTime @default(now())
    updatedAt    DateTime @updatedAt
}

model ContactUs {
    contactUsId String          @id @default(uuid())
    email       String?
    name        String
    message     String
    number      String
    subject     String
    status      ContactUsStatus @default(PENDING)
    createdAt   DateTime        @default(now())
    updatedAt   DateTime        @updatedAt
}

model Customer {
    customerId    String          @id @default(uuid())
    name          String
    phone1        String          @unique
    phone2        String?
    email         String?
    address       String?
    pincode       String
    city          String
    state         String
    booking       Booking[]       @relation(name: "BookingToCustomer")
    dob           DateTime?
    isMarried     Boolean         @default(false)
    isDelete      Boolean         @default(false)
    createdAt     DateTime        @default(now())
    updatedAt     DateTime        @updatedAt
    customerImage CustomerImage[] @relation(name: "CustomerToCustomerImage")
}

model CustomerImage {
    customerImageId String            @id @default(uuid())
    imageUrl        String
    customerId      String
    customer        Customer          @relation(name: "CustomerToCustomerImage", fields: [customerId], references: [customerId])
    type            CustomerImageType
    createdAt       DateTime          @default(now())
    updatedAt       DateTime          @updatedAt
}

model Expense {
    expenseId         String   @id @default(uuid())
    landPurchase      Int
    nonAgricultural   Int
    planningAndLayout Int
    landDevelopment   Int
    brokerage         Int
    landVisitCharge   Int
    projectId         String   @unique
    project           Project  @relation(name: "ExpenseToProject", fields: [projectId], references: [projectId])
    createdAt         DateTime @default(now())
    updatedAt         DateTime @updatedAt
}

model Festival {
    festivalId   String   @id @default(uuid())
    url          String
    thumbnailImg String
    description  String
    title        String
    isLatest     Boolean  @default(false)
    createdAt    DateTime @default(now())
    updatedAt    DateTime @updatedAt
}

model IBankPayment {
    paymentId     String      @id @default(uuid())
    installmentId String
    installment   Installment @relation(name: "IBankPaymentToInstallment", fields: [installmentId], references: [installmentId])
    bankName      String
    accountNumber String
    amount        Float
    createdAt     DateTime    @default(now())
    updatedAt     DateTime    @updatedAt
}

model Installment {
    installmentId  String           @id @default(uuid())
    bankPayment    IBankPayment[]   @relation(name: "IBankPaymentToInstallment")
    cashPayment    ICashPayment[]   @relation(name: "ICashPaymentToInstallment")
    chequePayment  IChequePayment[] @relation(name: "IChequePaymentToInstallment")
    bookingId      String
    booking        Booking          @relation(name: "BookingToInstallment", fields: [bookingId], references: [bookingId])
    amount         Int
    paymentType    IPaymentType
    installmentNo  Int
    adminAccountId Int?
    adminAccount   AdminAccount?    @relation(name: "AdminAccountToInstallment", fields: [adminAccountId], references: [adminAccountId])
    penalty        Int?
    isDelete       Boolean          @default(false)
    createdAt      DateTime         @default(now())
    updatedAt      DateTime         @updatedAt
    upiPayment     IUpiPayment[]    @relation(name: "IUpiPaymentToInstallment")

    @@unique([bookingId, installmentNo])
}

model ICashPayment {
    paymentId     String      @id @default(uuid())
    installmentId String
    installment   Installment @relation(name: "ICashPaymentToInstallment", fields: [installmentId], references: [installmentId])
    amount        Float
    createdAt     DateTime    @default(now())
    updatedAt     DateTime    @updatedAt
}

model IChequePayment {
    paymentId     String      @id @default(uuid())
    installmentId String
    installment   Installment @relation(name: "IChequePaymentToInstallment", fields: [installmentId], references: [installmentId])
    chequeNumber  String
    bankName      String
    amount        Float
    createdAt     DateTime    @default(now())
    updatedAt     DateTime    @updatedAt
}

model IUpiPayment {
    paymentId     String      @id @default(uuid())
    installmentId String
    installment   Installment @relation(name: "IUpiPaymentToInstallment", fields: [installmentId], references: [installmentId])
    upiId         String
    amount        Float
    createdAt     DateTime    @default(now())
    updatedAt     DateTime    @updatedAt
}

model MiscExpense {
    miscExpenseId String   @id @default(uuid())
    expenseName   String
    cost          Int
    projectId     String
    project       Project  @relation(name: "MiscExpenseToProject", fields: [projectId], references: [projectId])
    createdAt     DateTime @default(now())
    updatedAt     DateTime @updatedAt
}

model Otp {
    otpId      Int      @id @default(autoincrement())
    email      String
    otp        Int
    createdAt  DateTime @default(now())
    expiryTime DateTime
}

model Permission {
    permissionId Int      @id @default(autoincrement())
    label        String
    value        String
    role         Role[]   @relation(name: "PermissionToRole")
    group        String
    createdAt    DateTime @default(now())
    updatedAt    DateTime @updatedAt
}

model Role {
    roleId     Int          @id @default(autoincrement())
    permission Permission[] @relation(name: "PermissionToRole")
    label      String
    value      String
    createdAt  DateTime     @default(now())
    updatedAt  DateTime     @updatedAt
    user       User[]       @relation(name: "RoleToUser")
}

model ProjectImages {
    projectImageId String           @id @default(uuid())
    projectId      String
    type           ProjectImageType
    url            String
    project        Project          @relation(name: "ProjectToProjectImages", fields: [projectId], references: [projectId])
    createdAt      DateTime         @default(now())
    updatedAt      DateTime         @updatedAt
}

model UpiPayment {
    paymentId String   @id @default(uuid())
    bookingId String
    booking   Booking  @relation(name: "BookingToUpiPayment", fields: [bookingId], references: [bookingId])
    upiId     String
    amount    Float
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}

model User {
    userId    String   @id @default(uuid())
    name      String
    email     String   @unique
    phone     String
    password  String
    address   String?
    isAdmin   Boolean  @default(false)
    roleId    Int
    role      Role     @relation(name: "RoleToUser", fields: [roleId], references: [roleId])
    isDelete  Boolean  @default(false)
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}

enum PaymentStatus {
    PENDING
    PARTIAL
    CANCEL
    COMPLETED
}

enum PaymentType {
    CHEQUE
    UPI
    CASH
    BANK_TRANSFER
}

enum ContactUsStatus {
    PENDING
    COMPLETED
}

enum CustomerImageType {
    AADHAR_FRONT
    AADHAR_REAR
    PAN
    PHOTO
}

enum IPaymentType {
    CHEQUE
    UPI
    CASH
    BANK_TRANSFER
}

enum GroupType {
    ADMIN_ACCOUNT
    BOOKING
    PROJECT
    CUSTOMER
    EXPENSE
    ROLE
    USER
    INSTALLMENT
    REFERRAL
}

enum ProjectStatus {
    ACTIVE
    COMPLETED
    UPCOMING
}

enum ProjectImageType {
    PLANNING
    SITE
    HAPPY_CUSTOMER
}
