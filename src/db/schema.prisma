// *** GENERATED BY PRISMIX :: DO NOT EDIT ***
datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

generator client {
    provider        = "prisma-client-js"
    previewFeatures = ["fullTextIndex"]
}

model AdminAccount {
    adminAccountId Int       @id
    name           String
    bankName       String
    balance        Float
    createdAt      DateTime  @default(now())
    updatedAt      DateTime  @updatedAt
    booking        Booking[] @relation(name: "AdminAccountToBooking")
}

model BankPayment {
    paymentId     String   @id @default(uuid())
    bookingId     String
    booking       Booking  @relation(name: "BankPaymentToBooking", fields: [bookingId], references: [bookingId])
    bankName      String
    accountNumber String
    amount        Float
    createdAt     DateTime @default(now())
    updatedAt     DateTime @updatedAt
}

model Booking {
    bookingId      String          @id @default(uuid())
    bankPayment    BankPayment[]   @relation(name: "BankPaymentToBooking")
    projectId      String
    project        Project         @relation(name: "BookingToProject", fields: [projectId], references: [projectId])
    address1       String
    address2       String?
    pincode        String
    area           Float
    totalAmt       Float
    paidAmt        Float
    remainAmt      Float
    installmentAmt Float
    paymentType    PaymentType     @default(CASH)
    paymentStatus  PaymentStatus   @default(PARTIAL)
    customerId     String
    customer       Customer        @relation(name: "BookingToCustomer", fields: [customerId], references: [customerId])
    adminAccountId Int
    adminAccount   AdminAccount    @relation(name: "AdminAccountToBooking", fields: [adminAccountId], references: [adminAccountId])
    createdAt      DateTime        @default(now())
    updatedAt      DateTime        @updatedAt
    cashPayment    CashPayment[]   @relation(name: "BookingToCashPayment")
    chequePayment  ChequePayment[] @relation(name: "BookingToChequePayment")
    customerImage  CustomerImage[] @relation(name: "BookingToCustomerImage")
    upiPayment     UpiPayment[]    @relation(name: "BookingToUpiPayment")
}

model Project {
    projectId     String        @id @default(uuid())
    booking       Booking[]     @relation(name: "BookingToProject")
    name          String
    description   String?
    ownerName     String
    area          Int
    unit          String
    status        ProjectStatus @default(UPCOMING)
    address1      String
    address2      String?
    pincode       String
    createdAt     DateTime      @default(now())
    updatedAt     DateTime      @updatedAt
    parentId      String?
    parentProject Project?      @relation(name: "ParentProject", fields: [parentId], references: [projectId])
    childProject  Project[]     @relation(name: "ParentProject")
}

model Customer {
    customerId    String          @id @default(uuid())
    booking       Booking[]       @relation(name: "BookingToCustomer")
    firstName     String
    lastName      String
    aadharNo      String          @unique
    phone         String
    email         String?
    address       String?
    createdAt     DateTime        @default(now())
    updatedAt     DateTime        @updatedAt
    customerImage CustomerImage[] @relation(name: "CustomerToCustomerImage")
}

model CashPayment {
    paymentId String   @id @default(uuid())
    bookingId String
    booking   Booking  @relation(name: "BookingToCashPayment", fields: [bookingId], references: [bookingId])
    amount    Float
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}

model ChequePayment {
    paymentId    String   @id @default(uuid())
    bookingId    String
    booking      Booking  @relation(name: "BookingToChequePayment", fields: [bookingId], references: [bookingId])
    chequeNumber String
    bankName     String
    amount       Float
    createdAt    DateTime @default(now())
    updatedAt    DateTime @updatedAt
}

model CustomerImage {
    customerImageId String   @id @default(uuid())
    imageUrl        String
    customerId      String
    customer        Customer @relation(name: "CustomerToCustomerImage", fields: [customerId], references: [customerId])
    bookingId       String
    booking         Booking  @relation(name: "BookingToCustomerImage", fields: [bookingId], references: [bookingId])
    createdAt       DateTime @default(now())
    updatedAt       DateTime @updatedAt
}

model Referral {
    referralId String   @id @default(uuid())
    firstName  String
    lastName   String
    phone      String
    email      String?
    address    String?
    createdAt  DateTime @default(now())
    updatedAt  DateTime @updatedAt
}

model UpiPayment {
    paymentId String   @id @default(uuid())
    bookingId String
    booking   Booking  @relation(name: "BookingToUpiPayment", fields: [bookingId], references: [bookingId])
    upiId     String
    amount    Float
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}

enum PaymentStatus {
    PENDING
    PARTIAL
    COMPLETED
}

enum PaymentType {
    CHEQUE
    UPI
    CASH
    BANK_TRANSFER
}

enum ProjectStatus {
    ACTIVE
    COMPLETED
    UPCOMING
}
