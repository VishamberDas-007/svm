model AdminAccount {
    adminAccountId Int      @id @default(autoincrement())
    name           String
    bankName       String
    balance        Float
    accNo          String   @unique
    createdAt      DateTime @default(now())
    updatedAt      DateTime @updatedAt
}
